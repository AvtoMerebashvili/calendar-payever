import { AppointmentViewPipe } from './appointment-view.pipe';

describe('AppointmentViewPipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentViewPipe();
    expect(pipe).toBeTruthy();
  });
});
